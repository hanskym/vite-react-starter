import type { Config } from '@react-router/dev/config';

export default {
  appDirectory: 'src',
  ssr: false, // true for server-side rendering or false for client-only "SPA Mode"
} satisfies Config;
